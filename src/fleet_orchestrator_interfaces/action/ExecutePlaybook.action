# SPDX-License-Identifier: LicenseRef-Proprietary
#
# Execute a playbook-level command on a specific platform.
# This is a platform-facing interface (surrogate for DIU-provided playbooks).

string intent_id
string command_id
string[] vehicle_ids

# Generic parameters used in Sprint-1 demos.
# Convention:
# - north_m: forward displacement target (meters)
# - east_m:  right/strafe displacement target (meters)
float64 north_m
float64 east_m

# Optional freeform JSON for future extensibility (LLM outputs, vendor params)
string parameters_json
---
bool accepted
bool success
string reason
---
string state         # e.g., "accepted", "executing", "completed", "rejected"
float32 progress     # 0.0 - 1.0 (best-effort)
string message


